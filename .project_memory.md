# Project Memory: Stock_Price_Calculator

> Global memory: before starting or continuing this project, read `/Users/hannah/Documents/Codex/GLOBAL_MEMORY.md` first.


## Project Overview
- **Name**: Stock_Price_Calculator
- **Purpose**: A desktop application (Electron + HTML/CSS/JS) to calculate stock target prices based on intrinsic values.
- **Tech Stack**: Electron, Node.js, Vanilla HTML/CSS/JS.

## Naming & Formatting Conventions
- **Project Naming**: All project names must be capitalized for each word, and connected with underscores (`_`). Example: `Stock_Price_Calculator`.
- **Variables & Code**: Generally follow standard JS conventions unless specified otherwise, but project/folder names strictly follow the `Word_Word_Word` underscore format.

## Design & Style Preferences
- **UI Theme**: Light color scheme (`--bg-light: #f4f4f5`, `--card-bg: #ffffff`). Compact, modern, premium UI.
- **Layout**: Two-column (左右版面), calculation form on the left, history on the right. 
- **Window Management**: 
  - Fixed size (Width: 520, Height: 680) to bypass macOS memory and enforce a compact, "hugging" layout.
  - Floating above other apps (`alwaysOnTop: true`).
- **Icon Style**: Minimalist Flat Apple Style (like modern iOS flat design). 
  - *Icon Sizing (CRITICAL)*: Ensure exactly **15% transparent padding** around the core logo so it aligns perfectly with native macOS dock icons.
  - *Icon Processing*: When removing backgrounds, use an edge-inward flood fill to avoid hollowing out inner shapes of the same color.

## Current Status & Features
- **Current Version (v1.2.0)**: Major update introducing history grouping and enhanced UI management.
- **Key Features**:
  - **History Grouping**: History records are grouped automatically by stock symbol into modules.
  - **Dual-layer Drag-and-Drop**: Items can be sorted within their groups, and entire groups can be rearranged vertically.
  - **Global & Group Controls**: Dedicated 'Clear' buttons for clearing all history and 'X' buttons for deleting specific stock groups.
  - **Layout & Protection**: A dedicated, absolute-fixed 35px draggable top region protects the macOS window controls, ensuring the window can always be moved regardless of scroll position.
  - **Precision Alignment**: Consistent UI sizing (all delete/action icons scaled to 11px-14px) and right-aligned actions for a clean, unified aesthetic.
  - Click-to-populate: clicking a history record automatically fills the calculation form.
  - Reverse calculation: Base and Target prices can reverse-calculate each other while preserving the current percentage change.
  - Input auto-select: Clicking any text or number input auto-selects its content.

## Git & Deployment
- Source code is version controlled via Git.
- `npm run build` generates the macOS binary in `dist/mac-arm64/`.
